[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = [
    "E501",  # line too long — não bloqueante em DAGs/notebooks
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["plugins"]
